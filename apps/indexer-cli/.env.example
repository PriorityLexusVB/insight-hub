# .env template (DO NOT put real secrets in this file)
#
# Use it like this:
#   1) cp apps/indexer-cli/.env.example apps/indexer-cli/.env
#   2) Edit apps/indexer-cli/.env and set OPENROUTER_API_KEY=...
#   3) Run: pnpm -C apps/indexer-cli indexer:dev summarize --mode llm --max 1
#
# Never commit `.env` (it's already gitignored).

# OpenRouter API key (required for `summarize --mode llm`)
OPENROUTER_API_KEY=sk-or-v1-your-api-key-here

# Optional: override model (defaults to openai/gpt-4o-mini)
OPENROUTER_MODEL=openai/gpt-4o-mini
